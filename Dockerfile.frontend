FROM python:3.13-slim

WORKDIR /app

RUN pip install uv

COPY demo/pyproject.toml demo/uv.lock ./

RUN uv sync --frozen --no-dev

COPY demo/ .

RUN mkdir -p data

EXPOSE 8501

CMD ["sh", "-c", "unset STREAMLIT_SERVER_PORT && /app/.venv/bin/streamlit run frontend/app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true"]
